function matrix = inv_agr_components(matrix)
    % helps to find unknown matrix elements that can be found
    % input
    % matrix - matrix with famous components and zeros at unknown
    % output
    % matrix - converted input matrix

    s = size(matrix);
    % step 1 - check all elements and for every a(i,j)  get a(j, i)
    


end